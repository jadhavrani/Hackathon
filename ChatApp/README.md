Getting started 
 1. Go the directory i.e. /ChatApp
 2. Run below npm command on command line interface
	- npm install
 3. Run the app
	- node test.js
 4. If any module doesn't found, install it with below command
	- npm install <module>
 5. Rerun the project.
 5. Open http://localhost:3100 in browser.